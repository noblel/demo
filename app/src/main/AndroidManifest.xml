<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
  package="cn.noblel.demo">

  <application
    android:allowBackup="false"
    android:fullBackupContent="false"
    android:icon="@mipmap/ic_launcher"
    android:label="@string/app_name"
    android:name=".App"
    android:requestLegacyExternalStorage="true"
    android:roundIcon="@mipmap/ic_launcher_round"
    android:supportsRtl="true"
    android:theme="@style/AppTheme">
    <activity android:name=".cube.CubeActivity"></activity>
    <activity android:name=".MainActivity">
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />

        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>
    </activity>
    <activity android:name=".layout.LayoutActivity" />
    <activity android:name=".ffmpeg.FFmpegActivity" />
    <activity android:name=".viewpager.ViewPagerActivity" />
    <activity android:name=".texture.CameraTextureActivity" />
    <activity android:name=".surface.SurfaceViewActivity" />
    <activity android:name=".gl.GLViewActivity" />
    <activity android:name=".gl.preview.GLPreviewActivity" />
    <activity android:name=".bitmap.BitmapActivity" />
    <activity android:name=".texture.TextureViewActivity" />
    <activity android:name=".thread.ThreadHookActivity" />
    <activity android:name=".mediacodec.VideoDecodeActivity" />
    <activity android:name=".custom.CustomViewActivity" />
    <activity android:name=".animation.AnimationActivity" />
  </application>
  <uses-permission android:name="android.permission.INTERNET" />
  <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

  <uses-permission android:name="android.permission.CAMERA" />

</manifest>